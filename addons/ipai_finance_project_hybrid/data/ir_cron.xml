<?xml version="1.0" encoding="UTF-8"?>
<odoo>
  <data noupdate="1">
    <!-- Optional: keep schedules seeded if module is installed but data wiped -->
    <record id="ir_cron_ipai_finance_seed_if_empty" model="ir.cron">
      <field name="name">IPAI Finance: Seed if Empty</field>
      <field name="model_id" ref="model_ipai_finance_seed_service"/>
      <field name="state">code</field>
      <field name="code">model.seed_if_empty()</field>
      <field name="interval_number">1</field>
      <field name="interval_type">days</field>
      <field name="numbercall">-1</field>
      <field name="active">True</field>
    </record>
  </data>
</odoo>

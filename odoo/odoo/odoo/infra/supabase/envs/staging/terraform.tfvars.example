# Staging Environment Configuration
# Supabase control plane for staging/preview

environment            = "staging"
production_project_ref = "spdtwktxdalcfigzeqrz"  # TODO: Replace with staging project ref

# Secrets - set via environment variables:
# export TF_VAR_supabase_access_token="sbp_..."
# export TF_VAR_supabase_org_id="..."

# API Configuration (match production)
api_max_rows = 1000

# Auth Configuration (staging URLs)
site_url = "https://staging.erp.insightpulseai.com"
additional_redirect_urls = [
  "https://staging.control.insightpulseai.com/**",
  "http://localhost:3000/**"
]

# Plan

1. Keep main green
2. Enforce contracts via CI:
   - repo tree contract
   - schema artifacts (DBML/ORM map)
   - import header contracts
3. Build + publish IPAI Odoo image deterministically
4. Validate production health endpoints for gateway modules (OCR/SMS)

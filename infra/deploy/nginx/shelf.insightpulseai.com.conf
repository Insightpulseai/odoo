# Nginx Configuration - shelf.insightpulseai.com
# Status: PRODUCTION
# Last Updated: 2026-03-05
#
# Purpose: Reverse proxy to self-hosted Shelf.nu fork (asset management)
# Container: shelf-webapp-1 binds 127.0.0.1:3003 → 8080/tcp

upstream shelf {
    server 127.0.0.1:3003;
}

# HTTP → HTTPS redirect
server {
    listen 80;
    server_name shelf.insightpulseai.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS — reverse proxy to Shelf
server {
    listen 443 ssl http2;
    server_name shelf.insightpulseai.com;

    ssl_certificate /etc/letsencrypt/live/insightpulseai.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/insightpulseai.com/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;

    client_max_body_size 50M;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    location / {
        proxy_pass http://shelf;
        proxy_redirect off;
    }

    access_log /var/log/nginx/shelf.insightpulseai.com.access.log combined;
    error_log /var/log/nginx/shelf.insightpulseai.com.error.log warn;
}

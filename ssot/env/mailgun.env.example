# =============================================================================
# Mailgun Outbound — Canonical Environment Template
# =============================================================================
# Copy to .env.mailgun.local (gitignored) and fill in secrets.
# Never commit the actual values.
#
# SSOT: infra/dns/mailgun_mg_insightpulseai_com.yaml
# Runbook: docs/ops/email/MAILGUN_OUTBOUND_AUTOMATION.md
# =============================================================================

# Domain (canonical — do not change to .net)
MAILGUN_DOMAIN=mg.insightpulseai.com
MAILGUN_FROM=no-reply@mg.insightpulseai.com

# Mailgun API key (Primary or domain sending key)
# Source: Mailgun dashboard → API Security
# MAILGUN_API_KEY=

# Cloudflare API token — Zone:DNS:Edit on insightpulseai.com
# Source: Cloudflare → My Profile → API Tokens
# Regenerate if expired (returns HTTP 403 from CF API)
# CF_API_TOKEN=

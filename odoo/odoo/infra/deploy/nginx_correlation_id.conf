# Nginx Request Correlation ID Configuration
# Add this to your nginx site configuration to enable request tracking

# Generate request ID if not provided
map $request_id $reqid {
    default $request_id;
}

# Add to location block for /web:
# add_header X-Request-Id $reqid always;
# proxy_set_header X-Request-Id $reqid;

# Usage: 
# 1. Browser DevTools → Network → failing request → find X-Request-Id header
# 2. Server: docker logs odoo-prod | grep "<request-id>"
# 3. Exact request trace from browser to server logs

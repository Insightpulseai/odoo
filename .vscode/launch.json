{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Odoo (Server)",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/odoo-bin",
            "args": [
                "-c",
                "${workspaceFolder}/config/dev/odoo.conf"
            ],
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}

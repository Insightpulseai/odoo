# DEPRECATED: Replaced by infra-cloudflare-dns.yml
# This workflow pointed to infra/terraform/cloudflare/insightpulseai.com/ which does not exist.
# The correct Terraform root is infra/cloudflare/envs/prod/.
# Kept as a no-op to avoid breaking any branch protections that reference it.
name: Terraform - Cloudflare DNS (deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "⚠️  This workflow is deprecated."
          echo "Use 'infra-cloudflare-dns' instead."
          echo "See: .github/workflows/infra-cloudflare-dns.yml"

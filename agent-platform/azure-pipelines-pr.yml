# =============================================================================
# DEPRECATED — Re-homed to platform/ci/foundry-runtime-pr.yml
# =============================================================================
# Delete after updating AzDO pipeline ID 11 to point to:
#   platform/ci/foundry-runtime-pr.yml
# =============================================================================

trigger: none
pr: none

pool:
  vmImage: ubuntu-latest

steps:
  - script: |
      echo "This pipeline has moved to platform/ci/foundry-runtime-pr.yml"
      echo "Update AzDO pipeline ID 11 to point there."
    displayName: 'Pipeline moved — update AzDO definition'

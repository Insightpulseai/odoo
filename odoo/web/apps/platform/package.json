{
  "name": "platform",
  "version": "0.1.0",
  "private": true,
  "description": "Platform — secrets and environment configuration (not a deployable app)",
  "scripts": {
    "build": "echo 'No build step'",
    "test": "echo 'No test suite'"
  }
}

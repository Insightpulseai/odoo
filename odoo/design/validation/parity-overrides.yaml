# =============================================================================
# Design Parity Overrides Registry
# =============================================================================
# Every exception to parity-rules.yaml must be recorded here.
# Expired overrides fail CI. Missing overrides for exceptions fail CI.
#
# Required fields: id, owner, rationale, scope, replacement_rule,
#                  approval_date, review_date, expiry_date
# =============================================================================

version: 1
overrides: []
  # Example:
  # - id: override-001
  #   owner: platform-lead
  #   rationale: "Legacy Odoo QWeb templates use inline hex colors"
  #   scope: "addons/ipai/ipai_design_system/static/**"
  #   replacement_rule: "Migrate to CSS custom properties from design tokens"
  #   approval_date: "2026-03-17"
  #   review_date: "2026-06-17"
  #   expiry_date: "2026-09-17"

<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <!-- ========================================== -->
        <!-- Default Home Page Configuration -->
        <!-- ========================================== -->
        <!-- Sets custom home page as default landing page after login -->

        <!-- Configure system parameter for default home URL -->
        <record id="default_home_url_param" model="ir.config_parameter">
            <field name="key">web.base.url.redirect</field>
            <field name="value">/odoo</field>
        </record>

        <!-- Set custom home action as default for admin users -->
        <!-- Users will see the custom app grid after login -->
        <function model="res.users" name="write">
            <value eval="[ref('base.user_root'), ref('base.user_admin')]"/>
            <value eval="{'action_id': ref('ipai_default_home.action_ipai_home_page')}"/>
        </function>

    </data>
</odoo>

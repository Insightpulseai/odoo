# Databricks Asset Bundle: Foundation Python
# Purpose: Core Python library, medallion transforms, data quality checks, shared jobs
# Spec: spec/databricks-bundles-foundation/

bundle:
  name: foundation_python

include:
  - ../../shared/*.yml
  - resources/**/*.yml

sync:
  paths:
    - src
    - tests

variables:
  catalog:
    default: ${var.catalog}
  bronze_schema:
    default: ${var.bronze_schema}
  silver_schema:
    default: ${var.silver_schema}
  gold_schema:
    default: ${var.gold_schema}

targets:
  dev:
    default: true
    mode: development
    workspace:
      host: https://adb-7405610347978231.11.azuredatabricks.net
    variables:
      catalog: dev_ppm

  staging:
    mode: production
    workspace:
      host: https://adb-7405610347978231.11.azuredatabricks.net
    variables:
      catalog: staging_ppm

  prod:
    mode: production
    workspace:
      host: https://adb-7405610347978231.11.azuredatabricks.net
    variables:
      catalog: ppm

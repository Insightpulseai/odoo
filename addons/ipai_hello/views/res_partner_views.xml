<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Simple form extension: Add greeting field to Contact form -->
    <record id="view_partner_form_ipai" model="ir.ui.view">
        <field name="name">res.partner.form.ipai</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <!-- Add greeting field after name -->
            <field name="name" position="after">
                <field name="ipai_greeting" placeholder="Enter a custom greeting..."/>
            </field>
        </field>
    </record>

    <!-- List/tree view extension: Add greeting to contacts list -->
    <record id="view_partner_tree_ipai" model="ir.ui.view">
        <field name="name">res.partner.list.ipai</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//list//field[@name='complete_name']" position="after">
                <field name="ipai_greeting" optional="show"/>
            </xpath>
        </field>
    </record>

    <!-- ═══════════════════════════════════════════════════════════════════════
         Search Filter Extension
         NOTE: Commented out for simplified demo - requires additional fields
    ═══════════════════════════════════════════════════════════════════════ -->

    <!--
    <record id="view_partner_search_ipai" model="ir.ui.view">
        <field name="name">res.partner.search.ipai</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_res_partner_filter"/>
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='type_company']" position="after">
                <separator/>
                <filter string="High Engagement" name="high_engagement"
                        domain="[('ipai_score_category', '=', 'high')]"/>
                <filter string="Needs Attention" name="low_engagement"
                        domain="[('ipai_score_category', '=', 'low')]"/>
            </xpath>
            <xpath expr="//group" position="inside">
                <filter string="Segment" name="group_segment"
                        context="{'group_by': 'ipai_segment'}"/>
            </xpath>
        </field>
    </record>
    -->
</odoo>

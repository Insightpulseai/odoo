Resources
| where type =~ 'microsoft.keyvault/vaults'
| project
    id,
    name,
    type,
    location,
    resourceGroup,
    subscriptionId,
    sku = tostring(sku.name),
    tags
| order by name asc

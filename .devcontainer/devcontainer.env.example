# ==============================================================================
# Dev Container Environment Variables Template
# ==============================================================================
# Copy to .devcontainer/devcontainer.env and customize values
# File: .devcontainer/devcontainer.env is gitignored
#
# Usage:
#   cp .devcontainer/devcontainer.env.example .devcontainer/devcontainer.env
#   # Edit .devcontainer/devcontainer.env with your values
#   # Rebuild Dev Container: "Dev Containers: Rebuild Container"
# ==============================================================================

# --- Odoo Configuration ---
ODOO_DB=odoo_dev
ODOO_LOG_LEVEL=debug
ODOO_DEV_MODE=all
ODOO_PORT=8069
ODOO_LONGPOLL_PORT=8072

# --- PostgreSQL Configuration ---
POSTGRES_USER=odoo
POSTGRES_PASSWORD=odoo
POSTGRES_DB=odoo
POSTGRES_PORT=5433
POSTGRES_MAX_CONNECTIONS=100

# --- Database Names (created by post-create.sh) ---
DB_DEV=odoo_dev
DB_STAGE=odoo_stage
DB_PROD=odoo_prod

# --- Tool Versions (configurable) ---
# Leave empty for latest versions
UV_VERSION=
SPECIFY_VERSION=
NODE_VERSION=lts

# --- pgAdmin Configuration (optional, profile: tools) ---
PGADMIN_EMAIL=admin@localhost
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# --- Mailpit Configuration (optional, profile: tools) ---
MAILPIT_WEB_PORT=8025
MAILPIT_SMTP_PORT=1025

# --- Docker Compose Profiles ---
# Comma-separated list of profiles to enable by default
# Options: tools (pgAdmin + Mailpit), init, update
# COMPOSE_PROFILES=tools

# --- Development Preferences ---
# Enable verbose logging during setup
# VERBOSE=1

# --- Custom Addons Paths (advanced) ---
# Additional addon paths (comma-separated)
# CUSTOM_ADDONS_PATH=/custom/addons

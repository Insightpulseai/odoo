# Agent Library (IPAI) — Prompt Pack

This directory contains reusable prompts for coding agents (Claude Code, Codex, Gemini, CI runners).
Each prompt follows the execution template and outputs copy-paste-ready artifacts.

Conventions:

- No UI steps
- Idempotent scripts
- Provide apply/test/deploy/validate/rollback commands

<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2026 InsightPulse AI
     License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl).

     Replaces: mail_tracking/static/src/core/discuss/discuss_sidebar_mailboxes.xml
     Reason  : OCA template inherits from mail.Discuss.mobileTopbar which was
               renamed to mail.DiscussContent.mobileTopbar in Odoo 19.

     The first two templates (Mailbox.main, DiscussSidebarMailboxes) are
     unchanged from upstream; only the mobileTopbar t-inherit is corrected.
-->
<templates xml:space="preserve">
    <!--
        REMOVED: mail_tracking.Mailbox.main xpath targeting //span[@t-esc='mailbox.counter']
        The span element was removed in Odoo 19.0; this override caused an OWL OwlError.
        Counter styling (o-muted/o-danger) is no longer applied — acceptable regression.
    -->
    <t
        t-name="mail_tracking.DiscussSidebarMailboxes"
        t-inherit="mail.DiscussSidebarMailboxes"
        t-inherit-mode="extension"
    >
        <xpath expr="//div" position="inside">
            <Mailbox mailbox="store.failed" />
        </xpath>
    </t>
    <!--
        CHANGED: t-inherit was "mail.Discuss.mobileTopbar" (removed in Odoo 19).
        Correct parent in Odoo 19: mail.DiscussContent.mobileTopbar
    -->
    <t
        t-name="mail_tracking.DiscussContent.mobileTopbar"
        t-inherit="mail.DiscussContent.mobileTopbar"
        t-inherit-mode="extension"
    >
        <xpath expr="//div" position="inside">
            <t t-call="mail.MobileMailbox">
                <t t-set="mailbox" t-value="store.failed" />
            </t>
        </xpath>
    </t>
</templates>

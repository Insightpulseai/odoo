<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Settings action that opens directly to IPAI AI Settings -->
    <record id="action_ipai_ai_settings" model="ir.actions.act_window">
        <field name="name">AI Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">inline</field>
        <field name="context">{'module': 'ipai_settings_dashboard', 'bin_size': False}</field>
    </record>

    <!-- Menu under Settings -->
    <menuitem id="menu_ipai_settings_root"
              name="IPAI"
              parent="base.menu_administration"
              sequence="90"/>

    <menuitem id="menu_ipai_ai_settings"
              name="AI Settings"
              parent="menu_ipai_settings_root"
              action="action_ipai_ai_settings"
              sequence="10"/>
</odoo>

/** @odoo-module **/

// Minimal: ensure tokens apply everywhere.
// Later: read user preference (dark/light) from ir.config_parameter or user context.
document.documentElement.dataset.ipaiTheme = "fluent2";

# Tests for ipai_llm_supabase_bridge
#
# Layer 2 (Odoo TransactionCase) smoke tests — requires Odoo test runner:
#   python odoo-bin --test-enable --test-tags ipai_llm_supabase_bridge -d odoo_dev
#
# Test cases: S1–S4 in test_install_smoke.py

from . import test_install_smoke

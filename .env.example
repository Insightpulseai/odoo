# =============================================================================
# Environment Variables Template for Odoo CE 18 + IPAI Stack
# =============================================================================
# Copy this file to `.env.production` and fill in your actual values.
# 
# IMPORTANT SECURITY NOTES:
# 1. Secrets (service keys, tokens) are read via os.getenv() in modules
# 2. Non-secrets (URLs, toggles) can be stored in ir.config_parameter
# 3. NEVER commit actual secrets to git
# 4. NO Notion/Concur/Cheqroom/SAP integration configs (we clone SaaS UX natively)
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
POSTGRES_PASSWORD=your_secure_db_password_here
ODOO_ADMIN_PASSWD=your_secure_admin_password_here

# -----------------------------------------------------------------------------
# IPAI Stack Integration (YOUR PLATFORM SERVICES)
# These are InsightPulse AI infrastructure services, not external SaaS products
# -----------------------------------------------------------------------------

# Supabase (PostgreSQL + Edge Functions + Storage)
SUPABASE_URL=https://xkxyvboeubffxxbebsll.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
SUPABASE_ANON_KEY=your_anon_key_here

# n8n (Workflow Automation)
N8N_BASE_URL=https://n8n.insightpulseai.net
N8N_WEBHOOK_SECRET=your_webhook_secret_here
N8N_API_KEY=your_n8n_api_key_here

# Mattermost (Notifications & Ops Communications)
MATTERMOST_BASE_URL=https://mattermost.insightpulseai.net
MATTERMOST_BOT_TOKEN=your_bot_token_here
MATTERMOST_WEBHOOK_URL=your_webhook_url_here

# Superset (BI Dashboards & Analytics)
SUPERSET_BASE_URL=https://superset.insightpulseai.net
SUPERSET_EMBED_SECRET=your_embed_secret_here
SUPERSET_API_KEY=your_api_key_here

# -----------------------------------------------------------------------------
# Parity Modules Configuration (Optional Feature Toggles)
# These are NATIVE Odoo modules that clone SaaS UX, NOT external integrations
# -----------------------------------------------------------------------------
# Enable/disable specific parity modules (true/false)
ENABLE_EXPENSE_PARITY=true    # ipai_expense (Concur travel & expense parity)
ENABLE_ASSETS_PARITY=true     # ipai_assets (Cheqroom asset management parity)
ENABLE_PPM_PARITY=true        # ipai_ppm (Clarity PPM parity)
ENABLE_SRM_PARITY=true        # ipai_srm (SAP SRM supplier management parity)

# -----------------------------------------------------------------------------
# Usage Notes
# -----------------------------------------------------------------------------
# 1. Build Standard Profile:
#    docker build --build-arg PROFILE=standard -t odoo-ce:prod .
#
# 2. Build Parity Profile (includes all 27 ipai_* modules):
#    docker build --build-arg PROFILE=parity -t odoo-ce:enterprise-parity .
#
# 3. Run Production Stack:
#    docker-compose -f docker-compose.prod.yml up -d
#
# 4. Module Configuration:
#    - Secrets: Read via os.getenv() in Python code
#    - Non-secrets: Store in ir.config_parameter via Odoo Settings
#    - URLs/toggles: Can be in ir.config_parameter for runtime changes
#
# 5. ADR References:
#    - ADR-0001: No Notion integration (clone SaaS UX into Odoo CE/OCA)
#    - ADR-0002: Unified Dockerfile with build profiles
# =============================================================================

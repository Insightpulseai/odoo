# SOPS / age secrets (never commit private keys)
ops/secrets/age-key.txt
ops/secrets/*.key

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
!clients/**/lib/
!apps/**/lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
# Odoo
*.pyc
.odoo-bin
filestore/
sessions/
*.log
# Odoo addons - layered architecture: CE → OCA → IPAI
# Legacy flat structure (ipai_* and tbwa_* in addons root)
addons/*
!addons/ipai_*/
!addons/ipai_*/**
!addons/tbwa_*/
!addons/tbwa_*/**
# --- Deprecated addons quarantine (tracked) ---
!addons/_deprecated/
!addons/_deprecated/**
# --- IPAI custom modules (tracked) ---
!addons/ipai/
!addons/ipai/**

# --- Generated OCA aggregation output (git-aggregator target) ---
# SSOT: oca-aggregate.yml (tracked)
# Output: addons/oca/ (generated, never tracked)
addons/oca/
addons/OCA/
# Common ignores
addons/**/__pycache__/
addons/**/*.pyc
# Node.js
node_modules/
# Docker
.env
docker-compose.override.yml
# IDE
# .vscode/
!/.vscode/extensions.json
!/.vscode/settings.json
!/.vscode/tasks.json
!/.vscode/launch.json
!/.vscode/ipai_workspace.code-workspace
!/.vscode/README.md
.idea/
*.swp
*.swo
*~
.DS_Store
# Secrets
*.key
*.pem
*.crt
secrets/
.secrets
# Deployment
deploy/.env
deploy/*.local
deploy_m1.sh
deploy/*.log
deploy/*.backup.*
# OCA submodules (tracked separately)
oca/*
!oca/.gitkeep
# Logs
logs/
# Exception: canonical evidence logs under timestamped bundles only (20YY* prefix)
!web/docs/evidence/20*/**/logs/
!web/docs/evidence/20*/**/logs/*.log
# Temporary files
tmp/
temp/
*.tmp
*.bak
*.orig
# Database
*.sql
!db/**/*.sql
!infra/platform-kit/**/*.sql
!supabase/migrations/*.sql
!supabase/seed.sql
!supabase/seeds/*.sql
!supabase/seed/**/*.sql
!scripts/lakehouse/*.sql
!platform/db/migrations/*.sql
!scripts/supabase/sql/*.sql
!infra/lakehouse/**/*.sql
!tools/audit/*.sql
!tools/parity/*.sql
!scripts/sql/*.sql
!db/migrations/*.sql
!db/migrations/**/*.sql
!db/audit/*.sql
!db/process_mining/*.sql
!scripts/erd_dot.sql
!docs/data-model/*.sql
!tests/sql/*.sql
# Parity audit generated files
parity_report.json
*.db
*.sqlite
# Exception: Claude external memory
!.claude/project_memory.db
.claude/settings.local.json
# DB Inventory tool output
tools/db-inventory/output/
.env.smtp
*.env
!.env.local
!docker-compose.local.yml
!config/odoo.conf.local
*.code-workspace
!odoo.code-workspace
# Odoo import exports (generated, may contain sensitive data)
**/odoo_import_*.csv
**/odoo_import_*.xlsx
**/*_mapped.xlsx
**/*_PassA_*.csv
**/*_PassB_*.csv
# Raw source workbooks (sensitive data)
**/Month-end Closing Task and Tax Filing*.xlsx
data/source.xlsx
# Keep templates and sample files
!templates/odoo/import/*.csv
!data/user_map.csv
# --- local env / secrets ---
.env.*
!.env.example
!**/.env.example
.devcontainer/devcontainer.env
config/odoo.conf.local
.mcp.json
# --- local compose overrides ---
docker-compose.local.yml
docker-compose.*.local.yml
# --- node / next build ---
**/.next/
**/node_modules/
*.tsbuildinfo
.cursor/
# --- python ---
.venv/
# --- misc ---
# Incident snapshots (track only templates)
docs/incidents/*
!docs/incidents/templates/
# OCA local clones (dev-only, not committed)
sandbox/dev/oca-addons/
.env.local
!scripts/lib/
# ---- Large/derived artifacts (indexer-noise reduction) ----
# These bloat semantic search indexes with low-value content
**/.turbo/
**/.vercel/
**/coverage/
**/.nyc_output/
**/storybook-static/
**/.docusaurus/
**/*.min.js
**/*.min.css
**/*.bundle.js
**/*.chunk.js
**/vendor/
**/.terraform/
**/terraform.tfstate*
**/terraform.tfvars
**/.supabase-token
.ci_artifacts/
scripts/audit/.env
third_party/oca/
# MkDocs build output
site/
# LIB (Local Intelligence Brain)
.lib/
# Large brand mockup files (>1MB)
docs/brand/mockups/
# Graph generation output (ephemeral artifacts)
out/graphs/
# Terraform (Cloudflare SSOT)
infra/cloudflare/**/.terraform/
infra/cloudflare/**/.terraform.lock.hcl
infra/cloudflare/**/terraform.tfstate*
infra/cloudflare/**/tfplan
infra/cloudflare/**/terraform.tfvars
# Environment files (only .env is gitignored, templates are committed)
!.env.dev
!.env.stage
!.env.prod
 echo === CRITICAL: .env.production contains secrets and is tracked in git === echo  echo Adding to .gitignore now... echo .env.production echo Removing from git (keeping local file)... git rm --cached .env.production
*.local.json
# secrets (local only) - never commit
*.secrets.local
*.secrets.yaml

# Large evidence files (>100MB)
docs/evidence/**/*.txt
docs/evidence/**/*.log

# Archived compose files (rely on git history for restoration)
archive/compose/

# Superset local env (examples only in repo)
infra/superset/env/.env.superset
infra/superset/env/.env.*.local

# Cloudflare local env
.env.cloudflare

# Devcontainer local overrides
.devcontainer/.env

# Generic infra env files (examples only)
infra/**/env/.env
infra/**/env/.env.*.local
odoo-docs/

# ============================================================================
# Workspace Artifacts (Transient Development Files)
# ============================================================================
# Per MONOREPO_CONTRACT.md: These are ephemeral workspace artifacts
# that should never be committed
.cache/
_work/
artifacts/
sandbox/dev/.artifacts/
design-tokens/.figma-*
design-tokens/figma-*.json

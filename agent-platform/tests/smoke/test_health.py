"""Placeholder smoke test for runtime health check."""
import pytest


def test_health_endpoint_placeholder():
    """
    TODO: Implement when runtime-api is deployed.
    This test should:
    1. Start the runtime-api server (or use a test client)
    2. Call GET /health
    3. Assert status 200 and body contains {"status": "ok"}
    """
    # Placeholder assertion to keep the test suite valid
    assert True, "Health smoke test is a placeholder — implement when runtime-api is live"

# Content Model SSOT

Canonical content model definitions: page types, content blocks, metadata schemas, and editorial constraints.

## Convention

- Content models defined in YAML
- Each model declares required fields, optional fields, and validation rules
- Models are consumed by CMS integrations and static site generators

<!-- TODO: Define first content model when CMS integration is active -->

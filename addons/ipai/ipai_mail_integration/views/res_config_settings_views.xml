<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="res_config_settings_view_form_ipai_mail" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.ipai.mail</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="IPAI Mail" string="IPAI Mail" name="ipai_mail" groups="base.group_system">
                    <block title="Mail Gateway Configuration">
                        <setting string="Default Gateway" help="Select the default mail gateway for sending emails">
                            <field name="ipai_mail_gateway_id"/>
                        </setting>
                        <setting string="Email Tracking" help="Track email opens and clicks">
                            <div class="content-group">
                                <div class="row mt-2">
                                    <field name="ipai_mail_track_opens" class="col-2"/>
                                    <label for="ipai_mail_track_opens" string="Track Opens"/>
                                </div>
                                <div class="row mt-2">
                                    <field name="ipai_mail_track_clicks" class="col-2"/>
                                    <label for="ipai_mail_track_clicks" string="Track Clicks"/>
                                </div>
                            </div>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

</odoo>

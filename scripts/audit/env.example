# --- REQUIRED for API-based checks (leave blank to run DNS-only mode) ---

# Mailgun (private)
MAILGUN_API_KEY=
MAILGUN_REGION=us   # us|eu (set correctly)
MAILGUN_DOMAIN=mg.insightpulseai.net

# Supabase (choose one path)
SUPABASE_ACCESS_TOKEN=
SUPABASE_PROJECT_REF=        # e.g. abcdefghijklmnop
SUPABASE_API_URL=            # e.g. https://<ref>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=   # if you need to introspect protected endpoints

# Vercel (private)
VERCEL_TOKEN=
VERCEL_TEAM_ID=              # optional; if set, scopes to team

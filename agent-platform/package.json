{
  "name": "@ipai/agent-platform",
  "version": "0.1.0",
  "private": true,
  "description": "IPAI Agent Platform — runtime execution layer for Odoo Copilot and specialist agents",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf packages/*/dist apps/*/dist",
    "typecheck": "tsc --build",
    "test": "npm run test --workspaces --if-present",
    "lint": "echo 'lint placeholder'"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

# T002 — VizPayload core library + agent emit()
**Spec ref:** F-003
**Phase:** 1 — Core
**Predecessor:** T001
**Successor:** T004, T005, T006, T007 (parallel)
**Area path:** InsightPulseAI\Platform
**Estimate:** 6h

## Tasks
- [ ] `agent-platform/renderers/viz_payload.py`
  - `VizPayload` dataclass mirroring schema
  - `build_kpi_cards(data: dict) -> VizPayload`
  - `build_chart(type, labels, datasets, title) -> VizPayload`
  - `build_insight_pills(items: list[dict]) -> VizPayload`
  - `emit(payload: VizPayload, session_id: str) -> None` — posts to Service Bus topic
  - `validate(payload: VizPayload) -> bool` — runs ajv via subprocess
- [ ] `build_viz_payload()` added to all 8 agent skill modules:
  - `agents/skills/bank_recon/skill.py`
  - `agents/skills/finance_close/skill.py`
  - `agents/skills/tax_guru/skill.py`
  - `agents/skills/ap_invoice/skill.py`
  - `agents/skills/doc_intel/skill.py`
  - `agents/skills/pulser/skill.py`
  - `agents/skills/ces/skill.py`
  - `agents/skills/scout/skill.py`
- [ ] Unit tests for each agent's `build_viz_payload()` (8 test files)
- [ ] `pulser-viz validate` CLI command

## Done when
- All 8 agent skill modules import and call `build_viz_payload()` without error
- Schema validation passes on emitted payloads (contract test)
- Unit test coverage ≥ 90% on `viz_payload.py`

---

# T003 — viz_payload fixtures + Storybook mock data
**Spec ref:** F-001, F-002
**Phase:** 1 — Core
**Predecessor:** T001
**Successor:** T007
**Area path:** InsightPulseAI\Platform
**Estimate:** 3h

## Tasks
- [ ] `tests/fixtures/viz_payloads/` — 7 fixture files (one per viz_type)
  All use realistic IPAI/TBWA/Scout data (not Lorem Ipsum):
  - `kpi_cards.json` — Bank Recon: DSO=28d, DPO=42d, Days-to-close=5
  - `line_chart.json` — Scout: transaction volume Aug 19–Sep 15
  - `bar_chart.json` — Finance Close: budget vs actual by department
  - `grouped_bar.json` — Ces: SOV by channel (TV/Digital/OOH)
  - `donut_chart.json` — Scout: brand share Coca-Cola/Pepsi/Sprite/others
  - `sparkline.json` — Tax Guru: filing run timeline
  - `insight_pills.json` — Ces: win/risk/neutral campaign signals
- [ ] Storybook story file `web/src/components/viz/viz.stories.tsx` importing fixtures

## Done when
- All 7 fixtures pass `ajv validate`
- Storybook renders all 7 stories without error

---

# T004 — Foundry Code Interpreter chart renderer
**Spec ref:** F-004
**Phase:** 2 — Renderers
**Predecessor:** T002
**Successor:** T008
**Area path:** InsightPulseAI\Platform
**Estimate:** 8h

## Tasks
- [ ] `agent-platform/renderers/foundry_renderer.py`
  - `render_chart(payload: VizPayload) -> str` — returns blob URL
  - Calls Foundry Code Interpreter via Azure AI SDK
  - System prompt: IPAI design tokens, matplotlib style, 800×400px output
  - Uploads PNG to `stipaidevagent/charts/{agent}/{YYYY-MM-DD}/{id}.png`
  - Returns AFD URL: `https://erp.insightpulseai.com/charts/{path}`
  - DefaultAzureCredential — no hardcoded keys
- [ ] `pulser-viz render --target chart` CLI command
- [ ] Unit tests with mock Foundry SDK calls
- [ ] Integration test: real Code Interpreter call → verify blob URL reachable
- [ ] Bicep update: `stipaidevagent` container `charts` with AFD route

## Done when
- `pulser-viz render --payload tests/fixtures/viz_payloads/bar_chart.json --target chart`
  produces a reachable blob URL in < 8 seconds (p95)
- PNG uses IPAI colour palette (visual smoke test)
- All 5 chart types generate without error

---

# T005 — Teams Adaptive Card renderer
**Spec ref:** F-005
**Phase:** 2 — Renderers
**Predecessor:** T002
**Successor:** T008
**Area path:** InsightPulseAI\Platform
**Estimate:** 6h

## Tasks
- [ ] `agent-platform/renderers/teams_renderer.py`
  - `render_teams(payload: VizPayload) -> dict` — returns Adaptive Card dict
  - `kpi_cards` → ColumnSet with up to 3 columns per row (max 6 KPIs)
  - `chart types` → Image element with `blob_url` from T004
  - `insight_pills` → FactSet element
  - Delta color mapping: positive+up=Good, negative+down=Attention, neutral=Warning
- [ ] `pulser-viz render --target teams` CLI command
- [ ] Adaptive Card schema validation in unit test (Teams schema v1.5)
- [ ] Unit tests: all 7 viz_types → valid Adaptive Card JSON
- [ ] Manual smoke test: post card to TBWA\SMP test Teams channel

## Done when
- All 7 viz_types produce valid Adaptive Card JSON (validated by Teams schema validator)
- KPI delta colours correct for all delta sign combinations
- Card renders without error in Teams Desktop web client

---

# T006 — Odoo ipai_viz_chatter module
**Spec ref:** F-006
**Phase:** 2 — Renderers
**Predecessor:** T002
**Successor:** T008
**Area path:** InsightPulseAI\CoreOps
**Estimate:** 10h

## Tasks
- [ ] Module scaffold: `odoo/addons/ipai/ipai_viz_chatter/`
  - `__manifest__.py`: depends = `["mail", "base"]`, version `18.0.1.0.0`
  - `models/viz_log.py`: `ipai.viz.log` model (logs renders in Odoo)
  - `views/viz_log_views.xml`: `<list>` view (NOT `<tree>`), `view_mode="list,form"`
  - `security/ir.model.access.csv`
- [ ] QWeb templates `templates/viz_templates.xml`:
  - `ipai_viz_chatter.kpi_cards` — CSS grid of KPI tiles
  - `ipai_viz_chatter.chart_image` — `<img>` with `blob_url`
  - `ipai_viz_chatter.insight_pills` — `<span>` badges with win/risk/neutral classes
- [ ] `static/src/js/viz_chatter.js`:
  - Loads Chart.js 4.4.1 from cdnjs
  - Initialises canvas elements rendered by QWeb templates
  - Handles line/bar/donut chart types with IPAI tokens
- [ ] `static/src/scss/viz_chatter.scss`:
  - `.ipai-kpi-grid`, `.ipai-kpi-card`, `.ipai-pill-win`, `.ipai-pill-risk`, `.ipai-pill-neutral`
  - TBWA token variables
- [ ] `tests/test_viz_chatter.py` — Odoo test runner, tests model + template render
- [ ] Install test on `ipai-odoo-dev` ACA environment

## Done when
- Module installs on `ipai-odoo-dev` without errors
- Bank Recon agent can post a KPI card to `account.move` chatter thread
- Chart.js canvas renders inline (manual smoke test in Odoo web client)
- All views use `<list>` (grep verifies no `<tree>` anywhere in module)

---

# T007 — React viz components (ops-console)
**Spec ref:** F-007
**Phase:** 2 — Renderers
**Predecessor:** T002, T003
**Successor:** T008
**Area path:** InsightPulseAI\Platform
**Estimate:** 8h

## Tasks
- [ ] `web/src/components/viz/viz.types.ts` — TypeScript types from schema
- [ ] `VizRenderer.tsx` — switch on viz_type, renders correct sub-component
- [ ] `KpiCardGrid.tsx` — 1–6 metric tiles, delta colour logic
- [ ] `LineChart.tsx` — react-chartjs-2 wrapper, IPAI tokens
- [ ] `BarChart.tsx` — react-chartjs-2 wrapper
- [ ] `GroupedBarChart.tsx` — react-chartjs-2 wrapper (multi-dataset)
- [ ] `DonutChart.tsx` — react-chartjs-2 wrapper
- [ ] `Sparkline.tsx` — pure SVG, no Chart.js (perf)
- [ ] `InsightPills.tsx` — flex row of styled spans
- [ ] `web/src/components/viz/viz.test.tsx` — RTL tests for all components
- [ ] Storybook stories using T003 fixtures
- [ ] Integration: wire into Pulser chat panel in ops-console

## Done when
- All 7 viz_types render in Storybook without error
- Pulser chat sends a Finance Close query → KPI card appears inline in ops-console
- Component test coverage ≥ 90%
- Dark mode: all components readable on dark background

---

# T008 — Async queue worker + ACA Job + integration tests
**Spec ref:** F-008
**Phase:** 3 — Integration
**Predecessor:** T004, T005, T006, T007
**Successor:** none
**Area path:** InsightPulseAI\Platform
**Estimate:** 10h

## Tasks
- [ ] `agent-platform/renderers/queue_worker.py`
  - `process_message(msg: ServiceBusMessage) -> None`
  - Deserialize `viz_payload` from message body
  - Determine render targets from `cosmos-ipai-dev` session doc
  - If Teams target: call `foundry_renderer.render_chart()` → then `teams_renderer.render_teams()`
  - If Odoo target: call `odoo_renderer.render_odoo()` (no PNG needed)
  - If console target: call `console_renderer.render_console()` (no PNG needed)
  - Update `cosmos-ipai-dev` session doc with `blob_url`
  - Write `ipai_agent_run` row to Odoo via MCP
  - DLQ on 3 failures
  - Idempotency: skip if `id` already processed (check cosmos)
- [ ] `pulser-viz worker` CLI command
- [ ] `infra/azure/agent/viz-renderer-job.bicep`
  - ACA Job: `ipai-viz-renderer-job`
  - Trigger: Service Bus topic `viz-render`
  - MI: `id-ipai-agent-pulser-dev`
  - Env vars from Key Vault references
- [ ] Alert rule in `appi-ipai-dev-agent-sea`: queue depth > 1000 → PagerDuty
- [ ] Integration tests `tests/integration/test_end_to_end_render.py`:
  - Bank Recon agent posts KPI cards → verify Teams Adaptive Card JSON valid
  - Finance Close agent posts chart → verify blob URL reachable via AFD
  - Tax Guru agent posts insight pills → verify Odoo chatter message created
  - Scout agent posts donut chart → verify ops-console receives payload
- [ ] `azure-pipelines/viz-renderer.yml` — full pipeline: schema → unit → integration → bicep what-if → deploy

## Done when
- End-to-end test: agent emit → queue → worker → blob URL → client render in < 8s (p95)
- DLQ test: poisoned message after 3 retries lands in DLQ
- Idempotency test: duplicate payload IDs do not double-render
- ACA Job Bicep deploys without error (`az deployment group what-if` clean)
- All integration tests green in Azure Pipelines

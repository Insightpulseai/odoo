# Output Contract (Required)

Every response MUST include:

1. Minimal patch/diff (files + exact sections to change)
2. One Agent Relay Template prompt (implement end-to-end, open PR)
3. Verification checklist

Commands are included ONLY if explicitly requested.
DevOps changes MUST include rollback and post-deploy validation steps (non-command checklist if commands not requested).

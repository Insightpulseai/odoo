# Status: MERGED into spec/odoo-sh-azure-equivalent/

This spec bundle has been merged into `spec/odoo-sh-azure-equivalent/` as of 2026-03-28.

The unique content (authority model, control surface split, and implementation rules) has been
appended to `spec/odoo-sh-azure-equivalent/prd.md` under the "Staging Lifecycle Engine (merged)" section.

Do not add new content here. All future work should happen in `spec/odoo-sh-azure-equivalent/`.

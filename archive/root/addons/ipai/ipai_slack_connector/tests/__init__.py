# Tests for ipai_slack_connector
#
# Layer 2 (Odoo TransactionCase) tests — requires Odoo test runner:
#   python odoo-bin --test-enable --test-tags ipai_slack_connector -d odoo_dev
#
# Test cases: S1–S5 in test_slack_connector_notify.py

from . import test_slack_connector_notify

<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_hr_expense_refuse_wizard_form" model="ir.ui.view">
        <field name="name">hr.expense.refuse.wizard.form</field>
        <field name="model">hr.expense.refuse.wizard</field>
        <field name="arch" type="xml">
            <form string="Refuse Expense">
                <group>
                    <field name="sheet_id" invisible="1"/>
                    <field name="reason" placeholder="Enter the reason for refusing this expense report..."/>
                </group>
                <footer>
                    <button name="action_refuse" string="Refuse" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>
</odoo>

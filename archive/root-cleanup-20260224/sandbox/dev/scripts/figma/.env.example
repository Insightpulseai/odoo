# Figma Integration Environment Variables
# Copy to .env and fill in your values (NOT committed to git)

# Figma Personal Access Token
# Get from: https://www.figma.com/settings → Personal Access Tokens
# Local: Stored in macOS Keychain (use setup_figma_token.sh)
# CI: Stored in GitHub Secrets
FIGMA_ACCESS_TOKEN=figd_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Figma File Key
# Extract from Figma URL: https://www.figma.com/file/{FILE_KEY}/...
# Example: 7XCC5p6r9yDrMGCE9eI3LC
FIGMA_FILE_KEY=7XCC5p6r9yDrMGCE9eI3LC

# Optional: Override output directory
# Default: design-tokens/
DESIGN_TOKENS_DIR=design-tokens

# Optional: Override cache directory
# Default: .cache/
CACHE_DIR=.cache

# Optional: Force sync even if no changes detected
# Default: false
FORCE_SYNC=false

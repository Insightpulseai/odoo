<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">
    <!-- Plane Configuration Parameters (set these after installation) -->

    <!-- Example values - update after installation -->
    <record id="plane_workspace_slug" model="ir.config_parameter">
        <field name="key">plane.workspace_slug</field>
        <field name="value">insightpulseai</field>
    </record>

    <record id="plane_bir_project_id" model="ir.config_parameter">
        <field name="key">plane.bir_project_id</field>
        <field name="value"></field>  <!-- Set after creating project in Plane -->
    </record>

    <!-- Supabase parameters (if not already set) -->
    <record id="supabase_url" model="ir.config_parameter">
        <field name="key">supabase.url</field>
        <field name="value">https://spdtwktxdalcfigzeqrz.supabase.co</field>
    </record>

    <record id="supabase_service_role_key" model="ir.config_parameter">
        <field name="key">supabase.service_role_key</field>
        <field name="value"></field>  <!-- Set from vault/environment -->
    </record>
</odoo>

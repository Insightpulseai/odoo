<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <!--
        Node type documentation/reference data.
        This file provides documentation for available node types.
        Node types are defined in the model selection field, not as separate records.
        -->

        <!-- Example Journey Template (for demo purposes, noupdate=1 in production) -->
        <!--
        <record id="demo_journey_welcome" model="marketing.journey">
            <field name="name">Welcome Series</field>
            <field name="description">Automated welcome email series for new contacts</field>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="domain">[('customer_rank', '>', 0)]</field>
            <field name="state">draft</field>
        </record>
        -->
    </data>
</odoo>

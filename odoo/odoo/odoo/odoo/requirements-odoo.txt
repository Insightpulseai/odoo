# requirements-odoo.txt
# Odoo 19.0 CE core runtime dependencies
# Pin to match the official odoo:19.0 Docker image
#
# These are already included in the official image; listed here
# for reproducibility when building from source or custom images.
#
# See: https://github.com/odoo/odoo/blob/19.0/requirements.txt
# Do NOT duplicate Odoo's own requirements — use `-r` to pull them
# from the Odoo source tree if building from source:
#   -r /opt/odoo/requirements.txt
#
# If using the official Docker image, this file is a no-op (deps
# are baked in). Keep it as a placeholder for the layered include.

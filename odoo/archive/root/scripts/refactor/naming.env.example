# Naming Refactor Runner config
# Copy to: scripts/refactor/naming.env and edit as needed.

# Phase toggles (1=enable, 0=skip)
PHASE1_PKGS=1            # pkgs/ -> pkgs/
PHASE2_WEB=1             # web/ -> web/
PHASE2_DOCS_ARCH=1       # docs/arch/ -> docs/arch/
PHASE3_DB=0              # DISABLED: keep supabase/ for CLI compatibility
PHASE4_ADDONS_X=0        # DISABLED: ipai_* already well-namespaced
PHASE5_ENFORCE=1         # docs/NAMING.md + CI gate

# Compatibility mode
# If PHASE3_DB=1, create a symlink "supabase" -> "db" so Supabase CLI + existing tooling still works.
CREATE_SUPABASE_SYMLINK=1

# If PHASE4_ADDONS_X=1, create a symlink "addons/ipai" -> "addons/x" to keep older addon-paths working (optional).
CREATE_IPAI_SYMLINK=0

# Ripgrep patterns considered "legacy refs" (enforced by CI gate)
# NOTE: These are *textual references*, not directory existence.
# Adjusted: removed supabase/ and addons/ipai/ since we're keeping those
LEGACY_PATTERNS="(^|[^a-zA-Z0-9_])packages/|(^|[^a-zA-Z0-9_])apps/|docs/architecture/"

# Paths to ignore for legacy scanning (space-separated, globs ok for rg --glob)
# Example: "docs/evidence/** node_modules/**"
IGNORE_GLOBS="docs/evidence/** node_modules/** .git/** **/*.lock docs/NAMING.md scripts/refactor/naming.env"

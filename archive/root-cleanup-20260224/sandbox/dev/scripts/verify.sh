#!/usr/bin/env bash
# ═══════════════════════════════════════════════════════════════════════════════
# Local Verification Script
# ═══════════════════════════════════════════════════════════════════════════════
# Run all pre-deployment checks locally
# Usage: ./scripts/verify.sh
# ═══════════════════════════════════════════════════════════════════════════════
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$REPO_ROOT"

ERRORS=0

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "🔍 Dev Sandbox Verification"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

# ─────────────────────────────────────────────────────────────────────────────
# 1. Docker Compose Config
# ─────────────────────────────────────────────────────────────────────────────
echo "📦 Validating docker-compose.yml..."
if docker compose config > /dev/null 2>&1; then
  echo "   ✅ Valid syntax"
else
  echo "   ❌ Invalid docker-compose.yml"
  ERRORS=$((ERRORS + 1))
fi
echo ""

# ─────────────────────────────────────────────────────────────────────────────
# 2. Required Files
# ─────────────────────────────────────────────────────────────────────────────
echo "📄 Checking required files..."
for file in config/odoo.conf .env.example .gitignore REPORT.md; do
  if [ -f "$file" ]; then
    echo "   ✅ $file"
  else
    echo "   ❌ Missing: $file"
    ERRORS=$((ERRORS + 1))
  fi
done
echo ""

# ─────────────────────────────────────────────────────────────────────────────
# 3. Script Permissions
# ─────────────────────────────────────────────────────────────────────────────
echo "🔧 Checking script permissions..."
for script in scripts/dev/*.sh; do
  if [ -x "$script" ]; then
    echo "   ✅ $(basename "$script")"
  else
    echo "   ❌ Not executable: $script"
    ERRORS=$((ERRORS + 1))
  fi
done
echo ""

# ─────────────────────────────────────────────────────────────────────────────
# 4. Odoo Config Validation
# ─────────────────────────────────────────────────────────────────────────────
echo "⚙️  Validating Odoo config..."
if grep -q '\[options\]' config/odoo.conf; then
  echo "   ✅ Has [options] section"
else
  echo "   ❌ Missing [options] section"
  ERRORS=$((ERRORS + 1))
fi

if grep -q 'addons_path' config/odoo.conf; then
  echo "   ✅ Has addons_path configured"
else
  echo "   ❌ Missing addons_path"
  ERRORS=$((ERRORS + 1))
fi
echo ""

# ─────────────────────────────────────────────────────────────────────────────
# 5. Odoo 18 View Conventions
# ─────────────────────────────────────────────────────────────────────────────
echo "🔍 Checking Odoo 18 view conventions..."
if grep -r 'view_mode.*tree' addons/*/views/*.xml 2>/dev/null | grep -v '<tree' | grep -q .; then
  echo "   ❌ Found deprecated 'tree' in view_mode"
  grep -r 'view_mode.*tree' addons/*/views/*.xml | grep -v '<tree'
  ERRORS=$((ERRORS + 1))
else
  echo "   ✅ No deprecated 'tree' in view_mode"
fi
echo ""

# ─────────────────────────────────────────────────────────────────────────────
# 6. Addon Manifest Validation
# ─────────────────────────────────────────────────────────────────────────────
echo "📦 Validating addon manifests..."
for manifest in addons/*/__manifest__.py; do
  if [ -f "$manifest" ]; then
    if python3 -c "exec(open('$manifest').read())" 2>/dev/null; then
      echo "   ✅ $(dirname "$manifest" | xargs basename)"
    else
      echo "   ❌ Invalid syntax: $manifest"
      ERRORS=$((ERRORS + 1))
    fi
  fi
done
echo ""

# ─────────────────────────────────────────────────────────────────────────────
# 7. Optional: Shellcheck (if available)
# ─────────────────────────────────────────────────────────────────────────────
if command -v shellcheck &>/dev/null; then
  echo "🐚 Running shellcheck..."
  if shellcheck scripts/dev/*.sh 2>&1 | grep -v '^$'; then
    echo "   ⚠️  Shellcheck warnings (non-blocking)"
  else
    echo "   ✅ No shellcheck issues"
  fi
else
  echo "⚠️  Shellcheck not installed (skipping)"
fi
echo ""

# ─────────────────────────────────────────────────────────────────────────────
# Summary
# ─────────────────────────────────────────────────────────────────────────────
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
if [ $ERRORS -eq 0 ]; then
  echo "✅ All checks passed"
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  echo ""
  echo "Ready to:"
  echo "  ./scripts/dev/up.sh"
  exit 0
else
  echo "❌ Failed with $ERRORS error(s)"
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  exit 1
fi

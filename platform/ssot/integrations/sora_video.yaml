# Sora Video Generation Integration — SSOT
version: "1.0.0"
schema: ssot.integrations.v1
updated_at: "2026-03-31"

integration:
  id: sora_video
  name: "OpenAI Sora 2 (Video Generation)"
  vendor: openai
  status: planned
  category: video_generation

resources:
  - name: ipai-sora-dev
    type: azure_container_app
    rg: rg-ipai-dev-odoo-runtime
  - name: stipaidev
    type: storage_account
    services: [blob, queue]

runtime:
  job_table: platform.video_jobs
  database: pg-ipai-odoo

spec_path: platform/services/ipai-sora-dev/

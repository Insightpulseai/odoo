# Evals — odoo-report-customization

| Dimension | Pass criteria |
|-----------|--------------|
| Accuracy | QWeb directives used correctly; report action binds to correct model; template inheritance is valid |
| Completeness | All requested content added; report action created; paper format configured if needed |
| Safety | No core template replacements; t-raw used only for safe HTML; null data handled gracefully |
| Policy adherence | Inherited templates used; CSS class prefix o_<module>_ followed; manifest updated |
| Evidence quality | Render test with actual data captured; PDF generation success/failure logged |
| Upgrade safety | All changes via template inheritance; no direct core report file edits |

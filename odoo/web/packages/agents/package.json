{
  "name": "@ipai/agents",
  "version": "0.1.0",
  "private": true,
  "description": "Agent registry and handlers for the IPAI ops task bus",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-vm-modules $(which jest)"
  },
  "devDependencies": {
    "typescript": "^5"
  },
  "peerDependencies": {
    "@ipai/taskbus": "*",
    "@supabase/supabase-js": "^2"
  }
}

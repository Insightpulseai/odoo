# Odoo Docs Index — Insightpulseai/odoo

> Curated reference: official Odoo 19.0 documentation pages mapped to concrete repo actions.
> Generated: 2026-03-02 | Source: `ssot/docs/odoo_docs.yaml`
> Update: edit `ssot/docs/odoo_docs.yaml`, then re-curate this table.
> SaaS/EE-only pages are flagged — they inform parity mapping only, not requirements.

---

## Index

| Area | Doc page | URL | Why it matters | Repo action | Key setting/constraint | EE/SaaS |
|------|----------|-----|----------------|-------------|----------------------|---------|
| **Install/Maintain** | Source install | <https://www.odoo.com/documentation/19.0/administration/on_premise/source.html> | Python 3.12 + PostgreSQL 13+ requirements and pip install path for the DO droplet running Odoo CE 19.0. | `deploy/odoo-prod.compose.yml`, `scripts/odoo_install.sh` | `requirements.txt`, `python>=3.12`, `postgresql>=13` | |
| **Install/Maintain** | System configuration (deploy) | <https://www.odoo.com/documentation/19.0/administration/on_premise/deploy.html> | Canonical reference for all `odoo.conf` production params: workers, proxy_mode, list_db, dbfilter, gevent port, memory limits. | `config/odoo/odoo.conf`, `deploy/odoo-prod.compose.yml` | `workers`, `proxy_mode`, `list_db`, `dbfilter`, `limit_memory_hard`, `gevent_port=8072` | |
| **Install/Maintain** | Bugfix updates | <https://www.odoo.com/documentation/19.0/administration/on_premise/update.html> | Safe procedure for community bugfix releases without triggering a full upgrade — monthly patch cycle on the DO droplet. | `scripts/odoo_update.sh`, `.github/workflows/odoo-update.yml` | `git pull + odoo-bin --stop-after-init -u base` | |
| **Install/Maintain** | Command-line interface (CLI) | <https://www.odoo.com/documentation/19.0/developer/reference/cli.html> | Authoritative reference for all `odoo-bin` subcommands used in repo scripts: neutralize, scaffold, db dump/load, module install/upgrade, shell, cloc, test-tag filtering. | `scripts/odoo_*.sh`, `scripts/ci/*.sh` | `--test-tags`, `neutralize`, `scaffold`, `--workers`, `--proxy-mode`, `--db-filter` | |
| **Hosting** | nginx reverse proxy + HTTPS | <https://www.odoo.com/documentation/19.0/administration/on_premise/deploy.html> | Exact nginx upstream blocks for port 8069 (main) and 8072 (gevent/websocket), SSL settings, and `proxy_mode` — required for `erp.insightpulseai.com` nginx config. | `deploy/nginx/odoo.conf`, `deploy/nginx/ssl.conf` | `proxy_mode = True`, `upstream odoochat 127.0.0.1:8072`, `ssl_protocols TLSv1.2 TLSv1.3` | |
| **Hosting** | Packaged installers | <https://www.odoo.com/documentation/19.0/administration/on_premise/packages.html> | systemd service name, default `odoo.conf` path `/etc/odoo/odoo.conf`, and log locations used by deb package — relevant when the DO droplet runs the packaged installer. | `deploy/odoo-prod.compose.yml`, `scripts/odoo_service.sh` | `/etc/odoo/odoo.conf`, `/var/log/odoo/`, `systemctl restart odoo` | |
| **DB Mgmt** | Neutralized database | <https://www.odoo.com/documentation/19.0/administration/neutralized_database.html> | Exactly which params are disabled when neutralizing a prod DB copy for staging: scheduled actions, outgoing mail, payment providers, bank sync — critical before restoring prod dump to `odoo_dev`. | `scripts/odoo_neutralize.sh` | `odoo-bin neutralize --database odoo_dev` | |
| **DB Mgmt** | Upgrade | <https://www.odoo.com/documentation/19.0/administration/upgrade.html> | Official major-version upgrade path including getting an upgraded test DB via Odoo's upgrade service, running tests, and upgrading production — maps to staging → production promotion workflow. | `scripts/odoo_upgrade.sh`, `.github/workflows/odoo-upgrade-check.yml` | `pg_dump + upload to upgrade.odoo.com, then restore + --update all` | |
| **DB Mgmt** | Upgrade a customized database | <https://www.odoo.com/documentation/19.0/developer/howtos/upgrade_custom_db.html> | How to prepare and test `ipai_*` custom modules for a major-version upgrade, including how upgrade scripts interact with the upgrade service. | `addons/ipai/*/migrations/`, `scripts/odoo_upgrade_check.sh` | `migrations/$version/pre-*.py`, `post-*.py`, `end-*.py` | |
| **DB Mgmt** | Upgrade scripts | <https://www.odoo.com/documentation/19.0/developer/reference/upgrades/upgrade_scripts.html> | `migrate()` function contract and pre/post/end phase structure required for any `ipai_*` module storing data that needs migration between Odoo major versions. | `addons/ipai/*/migrations/$version/{pre,post,end}-*.py` | `def migrate(cr, version): ...`, module path: `$module/migrations/$version/` | |
| **DB Mgmt** | Upgrade utils | <https://www.odoo.com/documentation/19.0/developer/reference/upgrades/upgrade_utils.html> | Helper functions in Odoo's upgrade_utils for safe data manipulation inside migration scripts — reduces risk of broken references in `ipai_*` migrations. | `addons/ipai/*/migrations/` | `from odoo.upgrade import util; util.rename_field(), util.merge_records()` | |
| **Mail** | Manage outbound messages | <https://www.odoo.com/documentation/19.0/applications/general/email_communication/email_servers_outbound.html> | `ir.mail_server` model fields, FROM Filtering for Zoho Mail SMTP routing in `ipai_mail_bridge_zoho`, and Test Connection — authoritative for outgoing server record config. | `addons/ipai/ipai_mail_bridge_zoho/`, `config/odoo/mail_settings.yaml` | `ir.mail_server: smtp_host, smtp_port, smtp_user, smtp_pass, from_filter` | |
| **Mail** | Manage inbound messages | <https://www.odoo.com/documentation/19.0/applications/general/email_communication/email_servers_inbound.html> | `fetchmail_server` fields and `mail.catchall.domain` system parameter routing inbound emails to correct Odoo records — required for alias setup on `erp.insightpulseai.com`. | `config/odoo/mail_settings.yaml`, `scripts/odoo_mail_setup.sh` | `mail.catchall.domain`, `mail.catchall.alias`, `fetchmail_server: server, port, type` | |
| **Mail** | Email gateway | <https://www.odoo.com/documentation/19.0/administration/on_premise/email_gateway.html> | Postfix pipe configuration and `odoo-gemail` script enabling the DO droplet to receive inbound emails via MTA pipe — preferred for low-latency alias routing. | `deploy/postfix/main.cf`, `scripts/odoo_email_gateway.sh` | `odoo-bin --gevent-pipe`, `/etc/postfix/master.cf pipe command` | |
| **Mail** | Configure DNS records for email | <https://www.odoo.com/documentation/19.0/applications/general/email_communication/email_domain.html> | Required SPF, DKIM, DMARC records for `insightpulseai.com` to prevent Zoho-sent Odoo emails being marked spam — must be reflected in `infra/dns/subdomain-registry.yaml`. | `infra/dns/subdomain-registry.yaml` | `SPF: v=spf1 include:zoho.com`, DKIM TXT, DMARC policy | |
| **Mail** | mail.thread + mail.activity.mixin | <https://www.odoo.com/documentation/19.0/developer/reference/backend/mixins.html> | `mail.thread` and `mail.activity.mixin` inheritance pattern required by all `ipai_*` models needing chatter, followers, and activity support — prevents direct `message_post()` misuse. | `addons/ipai/*/models/*.py` | `_inherit = ['mail.thread', 'mail.activity.mixin']`, `message_follower_ids`, `activity_ids` | |
| **External API** | External RPC API (XML-RPC/JSON-RPC) | <https://www.odoo.com/documentation/19.0/developer/reference/external_rpc_api.html> | `authenticate()` → `uid` → `execute_kw()` flow used by n8n Odoo nodes and Python scripts. **DEPRECATION: both APIs removed in Odoo 20 (est. late 2026).** | `automations/n8n/workflows/*.json`, `scripts/odoo_api_client.py` | `/xmlrpc/2/common`, `/xmlrpc/2/object`, **removal in Odoo 20** | |
| **External API** | External JSON-2 API | <https://www.odoo.com/documentation/19.0/developer/reference/external_api.html> | Replacement for XML-RPC: API key + `Authorization: Bearer` header, modern JSON format — all new n8n integrations and Supabase edge functions should use this before Odoo 20. | `automations/n8n/workflows/*.json`, `supabase/functions/odoo-bridge/index.ts` | `Authorization: Bearer <api_key>`, endpoint `/web/dataset/call_kw` | |
| **Developer** | Module Manifests | <https://www.odoo.com/documentation/19.0/developer/reference/backend/module.html> | All `__manifest__.py` keys: name, version, depends, data, demo, installable, license — required for every `ipai_*` module and OCA validation. | `addons/ipai/*/__manifest__.py` | `version (X.Y.Z.Z.Z)`, `depends`, `installable=True`, `license='LGPL-3'` | |
| **Developer** | ORM API | <https://www.odoo.com/documentation/19.0/developer/reference/backend/orm.html> | Field types, decorators (`@api.depends`, `@api.onchange`, `@api.constrains`), recordset methods, `compute_sudo` — used in all `ipai_*` model implementations. | `addons/ipai/*/models/*.py` | `compute=`, `store=`, `compute_sudo=`, `@api.depends`, `@api.constrains`, `with_context()` | |
| **Developer** | Security in Odoo | <https://www.odoo.com/documentation/19.0/developer/reference/backend/security.html> | `ir.model.access.csv` format, `ir.rule` record rules syntax, groups attribute on fields — foundation of access control for every `ipai_*` module. | `addons/ipai/*/security/ir.model.access.csv`, `addons/ipai/*/security/security.xml` | `id,name,model_id/id,group_id/id,perm_read,perm_write,perm_create,perm_unlink` | |
| **Developer** | Data Files | <https://www.odoo.com/documentation/19.0/developer/reference/backend/data.html> | XML record/field/ref syntax, `noupdate` flag, CSV data format — governs how `ipai_*` modules load config and master data without overwriting customizations on upgrade. | `addons/ipai/*/data/*.xml`, `addons/ipai/*/data/*.csv` | `noupdate='1'`, `<record id='' model=''>`, `<field name='' ref=''/>` | |
| **Developer** | Actions | <https://www.odoo.com/documentation/19.0/developer/reference/backend/actions.html> | `ir.actions.act_window`, `ir.actions.server`, `ir.actions.act_url` — needed when `ipai_*` modules add menu items, server actions from buttons, or automated action sequences. | `addons/ipai/*/views/*.xml` | `ir.actions.act_window: res_model, view_mode, domain, context` | |
| **Developer** | View records and architectures | <https://www.odoo.com/documentation/19.0/developer/reference/user_interface/view_records.html> | `inherit_id`, `priority`, `mode=primary` on `ir.ui.view` — critical for `ipai_*` extending OCA or base Odoo views without replacing them. | `addons/ipai/*/views/*.xml` | `inherit_id`, `priority`, `mode=primary\|extension`, `xpath expr=`, `position=inside\|before\|after` | |
| **Developer** | Testing Odoo | <https://www.odoo.com/documentation/19.0/developer/reference/backend/testing.html> | `TransactionCase`, `HttpCase`, `tagged()` decorator, `--test-tags` CLI flag used in repo CI workflows to run module-specific tests without the full suite. | `addons/ipai/*/tests/test_*.py`, `.github/workflows/odoo-tests.yml` | `--test-tags +module_name`, `@tagged('post_install','-at_install')`, `TransactionCase` | |
| **Developer** | Coding guidelines | <https://www.odoo.com/documentation/19.0/contributing/development/coding_guidelines.html> | Naming conventions, file structure, XML ID patterns, Python style rules — `ipai_*` modules must follow these to pass OCA pre-commit hooks. | `addons/ipai/*/__manifest__.py`, `.pre-commit-config.yaml` | XML IDs: `module.group_name`; file order: `models > views > security > data` | |
| **Developer** | Scaffold a module | <https://www.odoo.com/documentation/19.0/developer/reference/cli.html> | `odoo-bin scaffold` generates the canonical `ipai_*` module skeleton — `scripts/odoo_scaffold.sh` wraps this to enforce `ipai_<domain>_<feature>` naming. | `scripts/odoo_scaffold.sh` | `odoo-bin scaffold <module_name> <addons_path>` | |
| **Odoo.sh Parity** | Branches (staging/production/dev) | <https://www.odoo.com/documentation/19.0/administration/odoo_sh/getting_started/branches.html> | Three branch stage types, build behaviors, worker allocation, cron throttling, and backup schedules our GitHub Actions + DO stack must replicate manually. | `.github/workflows/odoo-staging.yml`, `scripts/odoo_backup.sh` | staging: restore from prod; production: 7d+4w+3m backups; cron throttled on idle | EE/SaaS |
| **Odoo.sh Parity** | Submodules | <https://www.odoo.com/documentation/19.0/administration/odoo_sh/advanced/submodules.html> | How Odoo.sh auto-detects OCA submodules and adds them to `addons_path` — our equivalent is `.gitmodules` + `addons/oca/` bind-mount pattern. | `.gitmodules`, `deploy/odoo-prod.compose.yml` | `git submodule add -b <branch> <URL> addons/oca/<module>` | EE/SaaS |
| **Odoo.sh Parity** | Containers | <https://www.odoo.com/documentation/19.0/administration/odoo_sh/advanced/containers.html> | Odoo.sh container environment spec (Ubuntu, Python version, wkhtmltopdf) — our DO Docker image must match to avoid parity gaps. | `deploy/Dockerfile.odoo`, `deploy/odoo-prod.compose.yml` | `wkhtmltopdf`, `python3.12`, `node`, `lessc` versions must match | EE/SaaS |

# Supabase Edge Functions Environment Variables
# Copy to .env.local and fill in real values (NEVER COMMIT .env.local)

# =============================================================================
# SUPABASE (auto-injected in production, required for local dev)
# =============================================================================
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# =============================================================================
# ODOO CONNECTION (for seed-odoo-finance, shadow-odoo-finance)
# =============================================================================
ODOO_URL=https://erp.insightpulseai.net
ODOO_DB=production
ODOO_USER=seed_bot@insightpulseai.com
ODOO_PASSWORD=your-odoo-api-password

# =============================================================================
# AUTH TOKENS
# =============================================================================
# Shared secret for authenticating Edge Function calls
# Generate with: openssl rand -hex 32
SEED_RUN_TOKEN=your-random-64-char-token

# =============================================================================
# VAULT (optional, for HashiCorp Vault integration)
# =============================================================================
# VAULT_ADDR=https://vault.insightpulseai.net
# VAULT_TOKEN=your-vault-token

# =============================================================================
# LOCAL DEVELOPMENT
# =============================================================================
# Run locally with:
#   supabase functions serve <function-name> --env-file .env.local
#
# Set secrets for deployment with:
#   supabase functions secrets set KEY="value"
#
# List set secrets with:
#   supabase functions secrets list
# =============================================================================

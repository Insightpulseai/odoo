{
  "name": "@ipai/builder-evals",
  "version": "0.1.0",
  "description": "Eval runner: executes eval datasets from agents/evals/ against the runtime",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist",
    "test": "node --test dist/tests/"
  },
  "dependencies": {
    "@ipai/builder-contract": "0.1.0",
    "@ipai/builder-orchestrator": "0.1.0"
  },
  "files": ["dist"],
  "license": "LGPL-3.0"
}

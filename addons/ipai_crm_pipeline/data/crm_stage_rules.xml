<?xml version="1.0" encoding="UTF-8"?>
<odoo noupdate="1">
    <!-- Default stage rule configuration examples -->
    <!-- These will only be loaded on module install, not update -->

    <!-- Note: Actual stage configuration should be done via UI
         This file documents the expected setup -->

    <!-- Example: Qualified stage requires contact info -->
    <!--
    <record id="crm_stage_qualified_rules" model="crm.stage">
        <field name="name">Qualified</field>
        <field name="ipai_enforce_rules">True</field>
        <field name="ipai_sla_days">14</field>
        <field name="ipai_stage_color">#28a745</field>
        <field name="ipai_stage_icon">fa-check-circle</field>
        <field name="ipai_required_field_ids" eval="[
            (4, ref('base.field_crm_lead__contact_name')),
            (4, ref('base.field_crm_lead__email_from')),
        ]"/>
    </record>
    -->

</odoo>

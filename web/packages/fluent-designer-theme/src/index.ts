import { webLightTheme } from '@fluentui/react-components';

/**
 * Designer agent theme — extends Fluent webLightTheme.
 * Brand overrides go here through controlled token extension points.
 * Do not fork Fluent tokens into a parallel design system.
 */
export const designerTheme = {
  ...webLightTheme,
};

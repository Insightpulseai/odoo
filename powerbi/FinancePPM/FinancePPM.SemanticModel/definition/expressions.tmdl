/// Direct Lake source — Fabric Mirrored Database
/// Tables are auto-bound to OneLake delta tables from pg-ipai-odoo mirroring
/// No M query parameters needed (Direct Lake reads delta files directly)

expression DatabaseQuery =
	let
		Source = Sql.Database("", "odoo-mirror")
	in
		Source
	meta [IsParameterQuery=false]

# n8n Public API Configuration
N8N_BASE_URL=https://n8n.insightpulseai.com
N8N_API_KEY=__REPLACE_WITH_YOUR_API_KEY__

# Security: Set to 'true' only in trusted environments
# When false, only read operations (list, get) are allowed
ALLOW_MUTATIONS=false

# MCP Server Configuration
MCP_PORT=3100
MCP_LOG_LEVEL=info

# Optional: Request timeout in milliseconds
REQUEST_TIMEOUT=30000
